# fiducial_marker_core

Instructions:
1. Clone Repo
2. Build, for generic webcam node use following command: colcon build --cmake-args -DBUILD_ZED_NODE=OFF
3. Run: source install/setup.bash, ros2 run ar_pkg Aruco_Generic_Node
